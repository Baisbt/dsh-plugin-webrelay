# DESIGN.md — dsh-webrelay 产品设计

## 1. 一句话定位

dsh-webrelay 是 DeepSeek Harness (DSH) 的 Web 插件：在 DSH 界面右侧提供内置浏览器（同源中继嵌入外部 AI 对话网页），并在聊天输入框模型选择器左侧提供「闪电」按钮，一键完成 **提示词优化** 与 **与外部 AI 协作优化**——后者是一条双轮闭环：压缩对话上下文 → 第一次优化 → 发送给外部 AI → 等待并捕捉回复 → 结合回复做第二次优化 → 写入输入框。让外部 AI 参与进来，最终只把整理好的成品提示词交到用户手上。

## 2. 需求背景与目标

- 用户日常同时使用多个 AI 在线服务（DeepSeek、ChatGPT、豆包、通义千问、Gemini），频繁地复制粘贴上下文、切换窗口。
- 目标：
  1. 不离开 DSH 即可打开并操作外部 AI 网页；
  2. 一个按钮把输入框草稿改写成高质量提示词（不发送）；
  3. 一个按钮把草稿交给外部 AI 审阅，收回它的回答后再整合成终稿写回输入框——用户拿到的不是网页原文，也不是外部回答的复制品，而是消化了两者之后的成品提示词。
- 非目标（本期不做）：绕过登录/验证码、多账号管理、绕过站点反爬的风控对抗、非 Web 端（桌面客户端）支持。
- 明确的写边界：**只有「发送到浏览器」这一步会向外部站点写入**，且必须由用户在弹窗里点击；其余步骤全部只读。

## 3. 用户与核心场景

主要用户：在 DSH Web UI（127.0.0.1:3080）工作的个人用户，已在外部 AI 站点拥有账号并在浏览器里登录过。

| # | 场景 | 流程 |
|---|------|------|
| S1 | 打开内置浏览器 | 点击输入框右侧的「显示器」小按钮 → 右侧滑出浏览器面板 → 站点页签（来自配置文件）→ 页面经 relay 代理同源显示 |
| S2 | 优化提示词（不发送） | 在 DSH 输入框写草稿 → 点 ⚡ → 「优化提示词」→ 弹窗流式显示优化结果（可编辑）→ [撤回] 关闭即弃 / [重新生成] / [插入输入框]（仅写入草稿，不发送） |
| S3 | 与外部 AI 协作优化 | 右侧浏览器打开目标站点（内置或联动模式）→ 输入框写草稿 → 点 ⚡ → 「与外部 AI 协作优化」→ **S1 压缩上下文**（内部，静默）→ **S2 第一次优化**弹窗（初稿，可编辑，[撤回][重新生成]**[发送到浏览器 →]**）→ **S3 发送** → **S4 等待回复**（倒计时；超时可 [继续等待] / [采用已抓内容] / [撤回]）→ **S5 第二次优化**弹窗（终稿，可编辑，[查看外部回复][重新生成]）→ **S6** [插入输入框] 把终稿写入 DSH 草稿（不自动发送） |
| S4 | 引用历史捕获 | 浏览器面板「捕获历史」列表 → 点击一条 → 查看 / 复制 / 插入 DSH 输入框；DSH 会话中智能体可通过 `webrelay_captures` 工具检索历史捕获 |

## 4. 功能需求清单

| 编号 | 需求 | 验收标准 |
|------|------|----------|
| F1 | 右侧内置浏览器 | 面板可开合、可拖拽宽度；iframe 经 `/dsh-webrelay/proxy/...` 同源加载外部站点 |
| F2 | 站点识别 | iframe 当前 URL 的域名匹配配置文件 `sites.*.match` 时，面板与闪电菜单显示「已识别：XX」；未识别时选项二禁用并提示 |
| F3 | 闪电按钮 | 渲染在模型选择器左侧（`conversation.input.right` 槽）；点击弹出两项菜单：优化提示词 / 与外部 AI 协作优化 |
| F4 | 选项一：优化不发送 | 调用 DSH 宿主 LLM；结果仅在弹窗中展示（可编辑），提供 撤回 / 重新生成 / 插入输入框 三个动作；绝不自动发送 |
| F5 | 选项二步骤 1-2：压缩 + 初优化 | 读当前会话并压缩成背景摘要（不展示）；背景 + 草稿 → 待外发的提示词 A，弹窗可编辑；此阶段不对外部站点产生任何影响 |
| F5b | 选项二步骤 3-4：外发 + 收回 | A 经页面适配器填入外部站点输入框并提交（**唯一一次对外写入，用户点击触发**）；等待生成结束（默认上限 120s）后只读取回回复；超时不判失败，提供 继续等待（只读复查） / 采用已抓内容 |
| F5c | 选项二步骤 5-6：终优化 + 落笔 | 背景 + 原草稿 + 外部回复三者整合 → 终稿 C；弹窗可编辑、可重新生成；[插入输入框] 只写草稿不发送 |
| F5d | 轮次视角隔离 | 第一次优化按「读者是另一个模型」写（自带必要事实、无客套）；第二次优化按「消化外部回答」写（转写为指令、不搬运原文、不做元叙述） |
| F6 | 撤回与重新生成 | 各阶段弹窗都支持撤回（丢弃，不写不存）与重新生成（重新调用对应环节） |
| F7 | 回复抓取与保存 | 外部回复经用户确认/编辑后保存为 Markdown 文件（`$DSH_HOME/webrelay/captures/`），面板历史可查看 |
| F8 | 智能体工具 | 注册 `webrelay_captures` 工具：列出 / 读取捕获，供会话智能体引用 |
| F9 | 配置化 | 站点的地址与 DOM 选择器、等待上限、上下文条数全部在 `sites.yml`，用户可改；站点改版无需改代码 |
| F10 | 降级 | 站点嵌入失败（盾/登录墙/改版）时，面板提供「在系统浏览器打开」按钮；LLM 不可用时压缩/优化/清洗各有降级路径 |
| F11 | 附件提示（只提示，不代传） | 优化提示词时由优化器顺带判断该提示词是否可能依赖本机文件（要求读某文件 / 分析数据或日志 / 按截图还原 / 照已有代码改…）。若是，在弹窗里显示**一条**轻量提示：说明本次可能需要附件，并点明大致需要哪类材料（哪类文件、什么截图、什么日志）。**纯文案，无任何控件、无任何文件操作**——插件不检测、不读取、不上传；用户自行用外部浏览器/宿主页面自带的附件按钮上传。优化器判定无需附件时整块不渲染，不留无信息量的噪声 |

## 5. 边界与约束

- 遵守 DSH 官方插件机制（`dsh.bundle.patch` + `dsh.client`、`dsh plugin add/remove`、`dsh web --dump-config` 验证），不 patch 宿主源码，不覆盖其他插件占用的槽位。
- 兼容性：只使用 additive 槽位（`conversation.input.right`、`shell.overlay`），路由与样式全部 `dsh-webrelay-*` 命名空间隔离；卸载后宿主无残留。
- 安全：relay 代理仅监听回环、校验 Origin 同源、目标域名白名单（配置 `match`），不做通用开放代理；凭据不落盘（Cookie 仅内存 per-会话 jar）。
- **插件不承担文件采集与处理职责**：没有读盘代码，不存在扫描、枚举或后台读取目录的行为。附件只做「提示」——文案由优化器按需产出，实际上传由用户用页面自带的附件按钮完成。这样把复杂度与维护面压到最小。
- 合规：向外部站点的写入严格限定为「用户在弹窗中点击发送到浏览器」这一次动作；等待、捕捉、复查均为只读。用户需自行遵守目标网站条款，风险自担。

## 6. 交互原型（文字版）

```
输入框工具行右侧（模型选择器左边）：
  [⚡] [🖥]   …   [deepseek-v4.1-flash Off ▾] [↑]

⚡ 菜单：
  ┌──────────────────────────────────┐
  │ ✨ 优化提示词（不发送）             │
  │ ⚡ 与外部 AI 协作优化               │
  └──────────────────────────────────┘

第 1 次优化弹窗（S2，初稿）：
  ┌─ ⚡ 第 1 次优化（待外发）───────────┐
  │ 确认后发送给外部浏览器               │
  │ ┌────────────────────────────┐   │
  │ │ [初稿] ← 可编辑              │   │
  │ └────────────────────────────┘   │
  │ 📎 本次提示词可能需要附件——需要一份 │
  │    docker-compose.yml；请在外部浏览 │
  │    器用页面自带的附件按钮上传       │
  │   [撤回] [重新生成]  [发送到浏览器 →] │
  └──────────────────────────────────┘
  （提示条只在该提示词确实可能依赖文件时出现；不需要时整块不渲染。
    它是纯文案——点了也没有任何操作，上传要你自己在页面里做。）

等待弹窗（S3/S4）：
  ┌─ ⚡ 正在处理 ─────────────────────┐
  │   ◌ 正在发送到 DeepSeek，等待回复…  │
  │   最长等待 118s；超时后可继续等待     │
  │                      [取消]        │
  └──────────────────────────────────┘
  （超时后变为）
  ┌─ ⏱ 等待超时 ──────────────────────┐
  │  已抓到部分内容（823 字符）          │
  │  [撤回] [继续等待] [采用已抓内容 →]   │
  └──────────────────────────────────┘

第 2 次优化弹窗（S5，终稿）：
  ┌─ ✨ 第 2 次优化（终稿）─────────────┐
  │ 已吸收外部回复（3120 字符）并重写     │
  │ ┌────────────────────────────┐   │
  │ │ [终稿] ← 可编辑              │   │
  │ └────────────────────────────┘   │
  │ [撤回] [查看外部回复] [重新生成]      │
  │                        [插入输入框] │
  └──────────────────────────────────┘

捕获弹窗（查看历史存档 / 外部回复原文）：
  ┌─ 📥 外部 AI 回复 ────────────────┐
  │ [存档的文本] ← 可编辑             │
  │   [保存] [插入输入框] [复制] [关闭]  │
  └──────────────────────────────────┘
```

## 7. 里程碑

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M0 | 文档 + 脚手架 + 构建安装验证链路 | 进行中 |
| M1 | 闪电按钮 + 浏览器面板 + relay 代理（DeepSeek 同源显示） | 待办 |
| M2 | 选项一：提示词优化 + 撤回/重新生成 | 待办 |
| M3 | 选项二全链路（DeepSeek 实测） | 待办 |
| M4 | ChatGPT 实测 + 三站实验性 + 智能体工具 + README | 待办 |
